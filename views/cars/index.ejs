<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head.ejs") %>

    <body>
        <%- include("../partials/header.ejs") %>
        <h2>All Car Listings</h2>
            <main>
                 <div>   
                    <a href="/cars/new"><button>Create A New Car Listing</button></a>
                    <% for (car of cars) { %>
                        <article>
                            <a href="/cars/<%= car._id %>">
                                <img src=<%= car.image %> alt="car picture">
                                <h2>
                                    <%= car.model %> - <%= car.salvaged ? "Restored" : "Wrecked" %>
                                </h2>
                            </a>
                        </article>

                        <% } %>
                </div>
            </main>
    </body>

</html>
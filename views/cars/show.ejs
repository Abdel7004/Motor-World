<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head.ejs") %>

  <body>
    <%- include("../partials/header.ejs") %>
    <main>
      <div>
        <article>
          <h2><%= car.model %> - <%= car.salvaged ? "Restored" : "Wrecked" %></h2>
          <h3><%= car.price %></h3>
          <h3><img src=<%= car.image %> alt="car picture"></h3>
          <a href="/cars/<%= car._id %>/edit"><button>Edit</button></a>
          <form action="/cars/<%= car._id %>?_method=DELETE" method="POST">
            <input type="submit" value="Delete" class="delete-button"/>
          </form>
          <a href="/cars/"><button>Back to Main</button></a>
        </article>
      </div>
    </main>
  </body>
</html>